services:
  - type: web
    name: audio-preprocessing-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: app.py
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
